apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuard
spec:
  selector:
    matchLabels:
      app: kuard
  replicas: 2
  template:
    metadata:
      labels:
        app: kuard
    spec:
      containers:
      # - image: devopsctrain.azurecr.io/aks4:latest
      - image: devopsctrain.azurecr.io/aks4
        # imagePullPolicy: Always
        name: kuard
        ports:
        - containerPort: 8080